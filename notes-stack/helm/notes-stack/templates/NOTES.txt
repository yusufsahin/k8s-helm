1. Application is exposed through ingress-nginx NodePort.

{{- if .Values.ingress.host }}
Open: http://{{ .Values.ingress.host }}:30081{{ .Values.ingress.path }}
{{- else if .Values.access.publicHost }}
Open: http://{{ .Values.access.publicHost }}:30081{{ .Values.ingress.path }}
{{- else }}
Open: http://<node-ip>:30081{{ .Values.ingress.path }}
{{- end }}

API debug endpoint remains internal to the cluster.
